name: todo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.5.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.8
  flutter_slidable: ^3.1.1  # Updated to the latest version

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1  # Updated to the latest version
  build_runner: ^2.3.2  # Updated to the latest version
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
